"""FastAPI adapter for the OT Toolkit service contract.

Import ``app`` or ``create_app`` from ``ot_toolkit_backend.api.main`` so utility
module imports do not construct application state as a side effect.
"""
